#include <iostream>
#include <string>

using namespace std;

struct FamilyMember {
    string name;
    int age;
};

int main() {
 requested
    FamilyMember family[3] = {
        {"lilyahu", 67},
        {"lebron", 23},
        {"frenchmontana", 50}
    };

    string searchKey;
    cout << "Enter a key name to be searched: ";
    cin >> searchKey;

 image.png
    bool found = false;
    for (int i = 0; i < 3; i++) {
        if (family[i].name == searchKey) {
            cout << "That name was found and is aged " << family[i].age << endl;
            found = true;
            break; 
        }
    }

    if (!found) {
        cout << "Not found" << endl;
    }

    return 0;
}
